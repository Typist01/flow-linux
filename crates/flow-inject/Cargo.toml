[package]
name = "flow-inject"
version = "0.2.0"
edition = "2021"
license.workspace = true

[dependencies]
flow-config = { path = "../flow-config" }
thiserror = "2"
tokio = { version = "1", features = ["process", "time"] }
tracing = "0.1"
